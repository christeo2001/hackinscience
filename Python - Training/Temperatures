## Write functions to convert from Fahrenheit to Celsius and vice-versa

def fahrenheit_to_celsius(temp): // function that takes a input temp (fahrenheit)
    return (temp - 32) * 5/9

def celsius_to_fahrenheit(temp): // function that takes a input temp (celsius)
    return (temp * 9/5) + 32

if __name__ == "__main__":
    print(celsius_to_fahrenheit(100))
## 212.0
